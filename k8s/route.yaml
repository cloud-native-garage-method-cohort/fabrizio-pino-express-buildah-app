apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: express-buildah-app
spec:
  tls:
    termination: edge
  to:
    name: express-buildah-app
    kind: Service
    weight: 100
  wildcardPolicy: None
